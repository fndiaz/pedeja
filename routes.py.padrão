routers = dict(

    # base router
    BASE=dict(
        default_application='pedeja',
	applications=['projeto', 'admin',]
    ),

    pedeja=dict(
        default_controller='initial',
        default_function='principal',
        controllers=['initial', 'manager'],
        functions=['home', 'contact', 'about', 'user', 'log', 'delete', 
            'user', 'register', 'login', 'principal', 'log_out', 'log_in',
	    'cadastra_cliente',  'cadastra_categoria', 'guia', 'guia_item']
    )

)
